<div class="card">
    <div class="card-header">
        <h1>Show Role <%= @role.id %></h1>
    </div>
    <div class="card-body">
        <div class="form-group">
            <p>Role : <%= @role.name %></p>
        </div>
        <%= link_to 'Back', roles_path, :class => "btn btn-primary", :style => "color: #fff;"%>
    </div>
</div>